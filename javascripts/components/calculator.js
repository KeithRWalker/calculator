const addNumber = (num) => {
    console.log(num);
};

const setMathType = (newType) => {
    console.log(newType);
};

const calculate = () => {
    console.log('do math')
};

export default {addNumber, setMathType, calculate}